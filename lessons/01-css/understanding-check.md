# CSS Understanding Check

Answer these after we finish the walkthrough and example.

1. What does CSS stand for?
2. If HTML handles structure, what does CSS handle?
3. In a CSS rule, what does the selector do?
4. In `color: navy;`, what is the property and what is the value?
5. Why is using a separate `.css` file usually better than inline styles?
6. What does the "cascading" part of CSS refer to?
7. What is the difference between `margin` and `padding`?
8. When would you use a class selector like `.card` instead of an element selector like `p`?

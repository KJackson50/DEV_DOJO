# CSS Example

Open `index.html` in a browser.

Your first task:

1. Open `styles.css` and change the page colors.
2. Add spacing around the page content.
3. Style the resource list so it looks more intentional.
4. Refresh the browser after each change and observe what changed.

Focus on matching CSS rules to the HTML that is already there.
